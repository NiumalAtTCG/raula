<?xml version="1.0" encoding="utf-8"?>


<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="10dp"
    android:background="@color/matiria_card_bg"
    app:cardBackgroundColor="@color/matiria_card_bg"

    app:cardElevation="6dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="@color/matiria_card_bg"> <!-- Match the background color -->

        <!-- Feedback Message -->
        <EditText
            android:id="@+id/feedbackMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Type your feedback here..."
            android:inputType="textMultiLine"
            android:maxLines="5"
            android:minLines="3"
            android:textAlignment="center"
            android:textColorHint="@color/matiria_C_text"
            android:background="@android:color/transparent" /> <!-- Transparent background -->

        <!-- Rating -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="16dp"
            android:text="Rate your experience:"
            android:textSize="16sp"
            android:background="@android:color/transparent" /> <!-- Transparent background -->

        <RatingBar
            android:id="@+id/ratingBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="8dp"
            android:numStars="5"
            android:progressBackgroundTint="@color/mtrl_textinput_default_box_stroke_color"
            android:rating="0"
            android:stepSize="1"
            android:background="@android:color/transparent" /> <!-- Transparent background -->

        <!-- Submit Button -->
        <Button
            android:id="@+id/submitFeedbackButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:backgroundTint="@color/matiria_C_Secondry_Btn"
            android:text="Submit Feedback"
            android:textColor="@color/white" />

    </LinearLayout>
</androidx.cardview.widget.CardView>
